venv/
__pycache__/
*.pyc
.env
.idea/
*.log
data/*.json
!data/universe_snapshot.json
!data/positions_log.json
cache/
output/watchlist_*.md
sessions/

# Reversal tracking — only the three canonical .jsonl files are versioned.
# Backups and scratch dirs in that folder stay gitignored.
data/reversal_tracking/_pre_merge_backup/
data/reversal_tracking/*.bak
